#include<stdio.h>
#include<string.h>

int main(){
    int soma = 0, i;
    char palavra[51], c;

    scanf("%s %c", palavra, &c);

    for(i = 0; i<strlen(palavra); i++){
        if(palavra[i] == c){
            soma++;
        }
    }
        printf("%d", soma);
    
    return 0;
}
